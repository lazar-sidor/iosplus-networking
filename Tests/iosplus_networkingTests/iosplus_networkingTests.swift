import XCTest
@testable import iosplus_networking

final class iosplus_networkingTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(iosplus_networking().text, "Hello, World!")
    }
}
